apiVersion: ops.kubedb.com/v1alpha1
kind: MSSQLServerOpsRequest
metadata:
  name: msops-horizontal-down
  namespace: demo
spec:
  type: HorizontalScaling
  databaseRef:
    name: mssqlserver-ag
  horizontalScaling:
    replicas: 2